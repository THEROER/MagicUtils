package dev.ua.theroer.magicutils.platform;

/**
 * Player-originated message source type.
 */
public enum PlayerMessageType {
    /** Message sent via chat. */
    CHAT,
    /** Message sent as a command. */
    COMMAND
}
