# NeoForge

NeoForge support is minimal but provides config and logger integration.

```java
Platform platform = new NeoForgePlatformProvider();
ConfigManager configManager = new ConfigManager(platform);
```

Add `magicutils-neoforge` to your dependencies.
