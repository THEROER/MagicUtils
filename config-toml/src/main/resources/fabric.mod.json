{
  "schemaVersion": 1,
  "id": "magicutils-config-toml",
  "version": "${version}",
  "name": "MagicUtils Config TOML",
  "description": "TOML config format support for MagicUtils.",
  "authors": [
    "THEROER"
  ],
  "environment": "*",
  "depends": {
    "fabricloader": ">=0.14.0",
    "minecraft": "*"
  }
}
