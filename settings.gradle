pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://maven.fabricmc.net/' }
    }
}

rootProject.name = 'MagicUtils'
include 'platform-api', 'core', 'config', 'lang', 'logger', 'commands', 'placeholders', 'config-yaml', 'config-toml', 'platform-bukkit', 'platform-neoforge', 'platform-fabric', 'commands-fabric', 'logger-fabric', 'placeholders-fabric', 'fabric-bundle', 'processor'
