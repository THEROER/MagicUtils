pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = 'https://maven.fabricmc.net/' }
    }
}

rootProject.name = 'MagicUtils'
include 'platform-api', 'core', 'config', 'lang', 'logger', 'commands', 'placeholders', 'http-client', 'config-yaml', 'config-toml', 'platform-bukkit', 'platform-neoforge', 'platform-velocity', 'platform-fabric', 'commands-fabric', 'logger-fabric', 'placeholders-fabric', 'fabric-bundle', 'processor'
