pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url 'https://maven.fabricmc.net/' }
    }
}

rootProject.name = 'MagicUtils'
include 'platform-api', 'core', 'platform-bukkit', 'platform-neoforge', 'platform-fabric', 'processor'
