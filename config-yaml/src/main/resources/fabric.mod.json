{
  "schemaVersion": 1,
  "id": "magicutils-config-yaml",
  "version": "${version}",
  "name": "MagicUtils Config YAML",
  "description": "YAML config format support for MagicUtils.",
  "authors": [
    "THEROER"
  ],
  "environment": "*",
  "depends": {
    "fabricloader": ">=0.14.0",
    "minecraft": "*"
  }
}
